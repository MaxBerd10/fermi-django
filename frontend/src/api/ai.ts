/** Browser-facing AI client — sends requests to the server-side proxy. */
export {
  aiChat,
  staticAiReply,
  aiPathfinder,
  aiSearch,
  aiQabul,
  aiFaculty,
  aiReception,
  aiSummarize,
  type AiSource,
  type AiLink,
} from "@/lib/aiClient";
